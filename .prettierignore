# R1 pinned `family-board.jsx` here so `npm run format` could not reflow the
# prototype before R2 reviewed the move line by line. The file is gone
# (R2 backlog item 6) and the entry with it — everything under src/ is
# Prettier-formatted now.

# The extracted prototype stylesheet. It is a byte-for-byte fixture that
# src/styles/styles.contract.test.js compares BOARD_CSS against; reformatting
# it would break the comparison it exists to make.
src/test/fixtures/

# generated / vendored
node_modules/
dist/
coverage/
.vercel/
package-lock.json

# R0's and R14's prose — reflowing these churns the diff for no gain
PLAN.md
SCOPING.txt
README
